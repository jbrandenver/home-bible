# Home Folder (monorepo) — initial scaffold

This repo contains a pnpm monorepo scaffold for a Next.js web app, Expo mobile app, shared TypeScript packages, and Supabase migrations.

See `docs/` for architecture and security notes. The Home Automation module is
documented in `docs/HOME_AUTOMATION.md`.
