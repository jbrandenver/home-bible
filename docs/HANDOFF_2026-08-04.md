# Handoff — 2026-08-04

Read this first in a new session. It supersedes `SESSION_HANDOFF.md` (which is
itself marked superseded and describes the pre-launch world).

**State:** working tree clean, all work pushed to `main`, 262 tests passing,
migrations run to **040**. Site is live and money is live.

---

## What this day was

Post-launch hardening that turned into founder QA finding real bugs. The
morning was planned work (security advisors, reserved tables, chargeback
evidence, digest). The afternoon was Jesse clicking through the live app and
finding four classes of defect I would otherwise have shipped.

**The through-line, and the most important thing to carry forward:**

> Migration 033's column grants were verified with **impersonated rolled-back
> transactions at the data layer**. That cannot catch what the *client* does —
> a wrong column list, a wrong statement shape, or a write going to the wrong
> place entirely. **Every user-visible bug found today came through that gap.**

Verify through the app's real query strings and real flows, not just SQL.

---

## Shipped today

### Migrations
| # | What |
|---|---|
| 036 | Revoked EXECUTE on `enforce_property_allowance` (035 left it anon-callable); pinned `free_property_allowance` search_path |
| 037 | Commented the 7 reserved-but-unwritten tables |
| 038 | `record_entitlement_download()` — the Stripe chargeback evidence log, wired to handover print + archive download |
| 039 | Digest retry window + failed-send no longer suppresses the period |
| 040 | `list_pending_invitations_for_me()` / `accept_pending_invitation(uuid)` |

### Client fixes (each has a commit message with full reasoning)
- **Profile write failed on every sign-in** since 033. **No upsert can work on
  `profiles`** — PostgREST compiles `.upsert()` into
  `ON CONFLICT (id) DO UPDATE SET id = excluded.id, …`, and `id`/`email`/
  `created_at` have no UPDATE grant. Now UPDATE-then-INSERT.
- **Unresolved session read as "signed out"** → reads ran as `anon` → hard
  errors (031/033 revoked anon's grants). `guardedRead` retries once after an
  uncached session check and never leaks Postgres text.
- **Twelve `get*DataContext()` resolvers** decided demo-vs-account from the
  5-second `getCurrentUser()` cache. A stale `null` = demo mode = **the next
  save goes to localStorage**. Silent data loss, no error possible. All twelve
  now fall back to `getCurrentUserUncached()`.
- **`add-rooms` rendered its form in every `dataMode`**, so non-account modes
  fell through to `saveRooms()` (localStorage) then `resetForm()` — a cleared
  form that looked like a save.
- **Customer-facing errors rewritten** (`describeDataError`), 15 tests. An
  **allowlist**, not a blocklist — a test caught the blocklist version passing
  `ERROR 0x1f unexpected driver failure` to a customer.
- **Write controls hidden from viewers** (`usePropertyAccess`, `ViewOnlyNotice`).
  **Fails OPEN** — it is a courtesy, not a security control; RLS enforces.
  Layout carries the notice on every page; per-page button hiding done for 10.
- **Demo import** required an existing property, so it was unusable for a fresh
  signup. Now creates the home from the demo copy; `/welcome` opens on
  import-or-discard.
- **Invitations**: pending ones now appear on the dashboard (no link needed);
  refusal messages name the real cause.
- **Property switcher** shows from the first home, marks "· shared with me",
  carries "+ Add a home of my own".

---

## Open — Jesse

1. **Invitation emails** (decided, not built). Sharing sends no email at all;
   the owner must pass the link on. Needs a `send-invitation` edge function +
   Resend template. Domain `send.ourhomefolder.com` is verified and the digest
   proves the plumbing.
2. **CPA: state digital-goods tax** — last open money item. Sell US-only until
   answered.
3. **Cloudflare**: drop CSP `unsafe-inline` (turn off Bot Fight Mode / Rocket
   Loader — real trade-off, see docs/SECURITY.md); optional `www` → apex.
4. **Submit sitemap to Google Search Console.**
5. **Finish the QA pass** — still unclicked: viewer sees no contractor
   names/costs/private notes (**the guest-column enforcement from 031/033 has
   never been checked by a second account**), delete account, export archive
   zip, plate scan, portfolio, tenancy + condition report, compliance,
   seasonal plan, service call sheet.

**Closed today:** email confirmation is ON and verified in the data
(`confirmation_sent_at` populated, `email_confirmed_at` null on a new signup);
Resend SMTP for auth email; `ANTHROPIC_API_KEY` for the plate scanner.

## Open — engineering

- **Per-page write-control hiding** beyond the 10 done: maintenance, repairs,
  reminders, issues, compliance, tenancies, documents, portfolio, automation
  sub-pages, all detail pages. The layout banner covers everyone honestly in
  the meantime.
- **Audit trail misses blocked escalation attempts** — guard triggers `raise`,
  which rolls back any audit row in the same transaction. Needs an
  out-of-transaction write path.
- **Wire `entitlement_downloads` into any future paid artifact** (the two
  current ones are done).
- **Reserved tables**: `systems` and the automation history tables are
  commented, not built. Roadmap call.

---

## Gotchas worth not rediscovering

- **`supabase db push` is unsafe here** (001_ naming vs timestamp ledger). Use
  `supabase db query --linked -f <file>` then insert the
  `supabase_migrations.schema_migrations` row by hand. Always dry-run in a
  `begin; … rollback;` with a `raise exception` carrying the result.
- **Workers Builds deploys the Next app but NOT edge functions.**
  `supabase functions deploy <name>` is a separate required step.
- **`next build` runs ESLint**; build locally before pushing. Don't build while
  the dev server runs (`rm -rf apps/web/.next` first).
- **`digest_log` is UNIQUE on (user_id, kind, period_key)** — retries must
  upsert, not insert.
- **`getCurrentUser()` is fine for rendering, never for deciding where data is
  written.** Use `getCurrentUserUncached()` for that.
- The embedded browser stalls in Supabase's auth client — signed-in paths can't
  be verified there. Verify at the data layer, or have Jesse click it.
- **Don't ask Jesse to paste an `Authorization: Bearer` header** — ask yes/no.
  He pasted a live JWT once already.

## Tooling

`scratchpad/grantaudit.py` — resolves every select-list constant in the app and
diffs it against the live grant model, reads and writes, both directions.
Last run: **54 query sites, 7 restricted tables, zero mismatches.**
**Re-run after any column-grant migration.** Regenerate `grants.json` from
`information_schema.column_privileges` (query is in the session history).

## Verification habits that worked

- Reproduce as `set local role authenticated` / `anon` in a rolled-back txn —
  `anon` reproduces "permission denied for table X" exactly.
- `get_logs(service='postgres')` for the error; note ~10 min ingestion lag.
- Confirm writes landed by timing: a batch import writes rows milliseconds
  apart, a human clicking a wizard does not.
