# Supabase public client settings only (do NOT commit real secrets)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Never add SUPABASE_SERVICE_ROLE_KEY to frontend env files.

# App environment
NODE_ENV=development
